import { CheckResultsPathPipe } from './check-results-path.pipe';

describe('CheckResultsPathPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckResultsPathPipe();
    expect(pipe).toBeTruthy();
  });
});
