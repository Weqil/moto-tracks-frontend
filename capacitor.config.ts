import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'moto-tracks-frontend',
  webDir: 'www/browser'
};

export default config;
